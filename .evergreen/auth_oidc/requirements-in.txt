# Fail fast rather than building pydantic-core from source when it has no
# wheel for the host interpreter (e.g. CPython 3.14, DRIVERS-3649).
--only-binary=pydantic-core
boto3~=1.39.0
pyop~=3.4.0
PyOpenSSL>=23.2.0
azure-identity
azure-keyvault-secrets
